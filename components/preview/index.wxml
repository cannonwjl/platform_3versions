

<view catch:tap="onTap" class="container">
    <v-tag text="{{classic.goods_info.name}}" tag-class="tag" />
  <image class="music-img" src="{{classic.goods_info.fileid}}"></image>
  <view class="text">{{classic.goods_info.introdution}}</view>

</view>
 